name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: Avkroken/.github/.github/workflows/auto-release.yml@dc58997bf1b5731683c53b78dfb9f5b1254a937e
